# Webpage Snapshot 隐私政策

**更新日期：** [请填写生效日期]

## 概述

Webpage Snapshot 是一个 Chrome 扩展（Manifest V3），功能是让用户在网页上选择任意元素，并通过 SVG foreignObject + Canvas 重绘为 PNG 图片保存到本地设备。

本政策说明该扩展收集哪些信息、如何使用权限、如何处理数据。

## 核心声明

**Webpage Snapshot 不收集、不存储、不上传、不共享任何个人数据。** 所有处理均在用户浏览器本地完成。

## 数据收集

我们**不收集**任何数据，包括但不限于：

- 个人信息（姓名、邮箱、账号等）
- 浏览历史、访问的网址内容
- IP 地址、设备标识符
- 使用统计、行为分析数据

扩展**不使用**任何分析工具、跟踪器、广告 SDK 或 Cookie，也不加载任何远程代码。

## 权限用途说明

扩展申请了以下权限，仅用于实现核心功能：

1. **downloads（下载）** — 当用户明确点击「保存」时，将生成的 PNG 图片保存到用户本地的下载文件夹。图片不会上传到任何服务器。

2. **主机权限（`<all_urls>`）** — 用于两个目的：
   - 注入 content script，使用户可以在其访问的任何网页上选择元素进行截图；
   - 允许后台 service worker 抓取跨域图片资源并转换为 data URL，确保截图中的图片能够正确显示。

   该权限仅服务于截图功能本身，扩展不会读取、修改或拦截页面上与截图无关的任何数据。

## 数据处理方式

- 用户选择元素并截图时，扩展会克隆该元素的 DOM 子树并内联计算样式，这一过程**完全在用户浏览器内**完成。
- 图片资源被抓取后仅在**内存中**转换为 data URL，唯一用途是渲染截图。
- 最终的 PNG 通过 SVG foreignObject + Canvas 管线在本地生成，仅在**用户明确操作**时保存到本地下载文件夹或复制到剪贴板。
- 页面内容、截图及任何其他数据**永远不会离开用户的设备**。

## 第三方共享

我们不出售、出租或共享任何数据给第三方。扩展除抓取渲染截图所需的图片资源外，不发起任何网络请求。

## 数据保留与删除

扩展不保留任何数据。截图文件存储于用户自己设备的下载文件夹中，完全由用户控制——删除文件即永久删除。由于没有任何数据被传输给我们，我们的服务器上不存在任何用户数据。

## 儿童隐私

扩展不收集任何人的个人信息，包括 13 岁以下儿童的信息。

## 安全

所有处理均在用户本地设备上完成，不存在数据传输，因此不存在数据被拦截的风险。

## 政策变更

我们可能不时更新本政策，任何变更将反映在本页面中。

## 联系我们

如对本隐私政策有任何疑问，请联系：**[uo.is.zero@gmail.com](mailto:uo.is.zero@gmail.com)**
